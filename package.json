{
  "name": "almakhora-website",
  "version": "1.0.0",
  "scripts": {
    "dev": "echo \"Static site - no dev server\"",
    "build": "echo \"Building static site...\" && rm -rf public && mkdir -p public && cp *.html public/ && cp -r css js public/ && echo \"✓ HTML, CSS, JS copied\"",
    "postbuild": "./postbuild.sh",
    "netlify-build": "npm run build && npm run postbuild"
  }
}
